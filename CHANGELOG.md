# 更新记录

## 未发布

- 后台启动恢复改为分阶段降级，单个存储步骤失败只影响该步骤并输出错误，不再让整个消息队列永久失败。
- 探测订阅按两分钟过期，并在标签页关闭时回收，同时通知离屏文档丢弃对应探测；页面侧超过 45 秒没有结果的探测自动重发，不再长期停在“检测中”。
- 两个合并任务运行期间仍保留一条探测通道，画质探测不再被长合并完全阻塞。
- MAIN 世界的响应读取限制为三个并发、八秒期限和 6 MiB 排队上限，超期取消并逐条合并解析，避免无界克隆和解析占用页面。
- 打包先写入临时目录再原子替换 `dist`；发布校验比对完整文件集合与逐文件哈希，并固定权限、主机权限、内容安全策略和可访问资源；ZIP 条目排序且使用固定时间戳，产物可复现。
- 新增后台消息信任边界、启动恢复和探测订阅回收的回归测试。

## 1.2.0 - 2026-09-15

- 将媒体探测改为 32 KiB 有界 Range 读取，共享重叠请求，避免通用网络源预读大量视频负载。
- 全部候选与音视频轨道有界并行，保留原有采样数量；显示部分结果时明确标注确认中。
- 两条探测队列、可见视频优先、滚走取消、Worker 复用，解决慢视频阻塞后续视频的问题。
- 会话缓存保存十五分钟摘要与两分钟候选，跨 service worker 休眠及离屏文档关闭恢复。
- 避免重复 DOM 图标创建及无关页面样式变化触发全量播放器布局计算，页面更新调度从 100ms 缩到一帧。
- 独立项目迁至 `F:\x-video-downloader`，原路径仅保留安装兼容链接；固定 Node/pnpm 规范并新增 CI。
- 80ms 模拟网络往返基准：完整单媒体探测约 1.69s → 0.57s，读取字节减少约 86%；三媒体显示约 5.20s → 1.17s。基准严格校验优化前后全部候选和测量值一致。

## 1.1.1 - 2026-09-15

- 修复部分 X 播放器中画质/音质信息叠在视频左上角的问题。
- 按钮与信息栏使用独立的挂载位置：信息栏离开绝对定位、比例占位和裁剪层，在整个播放器下方占据正常文档高度，撑开视频与互动栏之间的区域。
- 布局尚未就绪时等待尺寸，播放器换层或重新显示后重新定位，保留已有任务状态。
- 新增 8 项真实 MV3 布局回归场景，覆盖深层旧版结构、尺寸不一致的比例层、普通布局、窄视口、重新挂载和隐藏后显示。

## 1.1.0 - 2026-09-15

- 下载按钮从 34px 调整为 28px，图标 16px，使用半透明背景和实时 backdrop blur。
- 点击按钮展开视频/音频菜单，支持键盘选择、Escape 和点击外部关闭，更新 Tooltip。
- 支持最高音质音轨选择，从 MP4/HLS 无损提取 M4A；保留编码、采样率、声道数，不混入视频轨。
- 视频下方自动显示最高画质和音质，以规范单位显示分辨率、帧率、编码、估计码率、采样率和声道数。
- 检查按可视区域调度，缓存两分钟，下载复用正在进行的检查；没有音轨时明确显示并禁用音频选项。
- 视频与音频任务分别去重，保留后台恢复、降级、取消和文件清理。
- 自动生成版本化安装 ZIP 和 SHA-256 校验文件。

## 1.0.0 - 2026-09-15

- 首个独立 MV3 版本，支持 X 新旧 GraphQL/SSR、视频卡片、最高可用 MP4/HLS 下载及音视频无损合并。
